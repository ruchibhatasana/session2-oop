session 1 

Q-1 Write a simple C program tasklist_c.c that allows you to add up to 5 tasks
(as strings) to a global array and print all tasks using a for loop.

ANS:-
#include <stdio.h>
#include <string.h>

char tasks[5][50];

int main()
{
    int i;

    printf("Enter 5 tasks:\n");

    for(i = 0; i < 5; i++)
    {
        printf("Task %d: ", i + 1);
        scanf("\n %s", tasks[i]);
    }

    printf("\nTask List:\n");

    for(i = 0; i < 5; i++)
    {
        printf("%d. %s\n", i + 1, tasks[i]);
    }

}


Q-2 Modify your tasklist_c.c to add a function markTaskDone(int index) that
 sets the selected task to 'DONE' in the array, then print the updated list.
 <br><br><em><strong>Hint:</strong> Use a separate status array or append ' 
 - DONE' to the task string.</em>
 
ANS:-
#include <stdio.h>
#include <string.h>

char tasks[5][50];

void markTaskDone(int index)
{
    strcpy(tasks[index], "DONE");
}

int main()
{
    int i;

    for(i = 0; i < 5; i++)
    {
        printf("Enter Task %d: ", i + 1);
        scanf("%s", tasks[i]);
    }

    markTaskDone(2);

    printf("\nTask List:\n");

    for(i = 0; i < 5; i++)
    {
        printf("%d. %s\n", i + 1, tasks[i]);
    }

}



Q-3:- Create a Task class in your favorite OOP language (Java, Python, or C++)
 with properties: title and isDone. Add a method markDone() that sets 
 isDone to true, and a method display() that prints the task with its status.
 
ANS:-//(c++ i can used)
#include <iostream>

using namespace std;

class Task
{
public:
    string title;
    string status;

    void markDone()
    {
        status = "DONE";
    }

    void display()
    {
        cout << "Task: " << title;
        cout << "Status: " << status;
    }
};

int main()
{
    Task t;

    t.title = "Homework";
    t.status = "Pending";

    t.display();

    t.markDone();

    cout << "\nAfter Done:\n";
    t.display();

}


Q-4:-Build a simple TaskList class/object that stores multiple Task objects 
and provides addTask(title), markTaskDone(index), and showTasks() methods.
Demonstrate adding 3 tasks, marking one as done, and displaying all tasks
with their statuses.

ANS:-
#include <iostream>
using namespace std;

class Task
{
public:
    string title;
    string status;
};

class TaskList
{
public:
    Task task[3];
    int count = 0;

    void addTask(string t)
    {
        task[count].title = t;
        task[count].status = "Pending";
        count++;
    }

    void markTaskDone(int i)
    {
        task[i].status = "DONE";
    }

    void showTasks()
    {
        for(int i = 0; i < count; i++)
        {
            cout << task[i].title;
            cout <<task[i].status;
        }
    }
};

int main()
{
    TaskList list;

    list.addTask("Homework");
    list.addTask("Study");
    list.addTask("Project");

    list.markTaskDone(1);

    list.showTasks();

    
}

Q-5:-Compare your procedural C version and OOP TaskList version: List 3
 problems you faced in the C version that were solved by using classes
 and methods in the OOP version.
 
ANS:-
C Version
 In the procedural C version, managing tasks was a little difficult. We had to
 use arrays and separate functions to store and manage tasks. Marking a task 
 as done required extra handling, and as the number of tasks increased, the 
 code became more difficult to organize and maintain.

OOP Version

 In the OOP version, these problems were solved using classes and methods. The
 Task class stores the task details, while the TaskList class manages multiple
 tasks. Methods like addTask(), markTaskDone(), and showTasks() make the 
 program more organized, simple, and easy to manage.
